{
  "name": "flatten-to-object",
  "version": "1.0.0",
  "description": "Flatten an array of objects to a single object",
  "main": "index.js",
  "scripts": {
    "test": "node test.js | tap-spec"
  },
  "keywords": [
    "array",
    "object",
    "flatten"
  ],
  "author": "Scott Corgan",
  "license": "MIT",
  "devDependencies": {
    "tap-spec": "^2.2.1",
    "tape": "^3.5.0"
  },
  "dependencies": {
    "lodash.assign": "^3.0.0",
    "lodash.reduce": "^3.0.0"
  }
}
